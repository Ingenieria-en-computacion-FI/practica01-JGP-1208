//* Salida:
&a[0] = 5000
&a[3] = 5012

Calcula:
tamaño del tipo
R= Para esto podemos ver que hay una diferencia de 12 unidades entre &a[0] = 5000 y &a[3] = 5012, por lo tanto se puede decir que hay ua diferencia de 4 Bytes, que es el tamaño de cada elemento

dirección de a[5]
R= Su dirección seria &a[5] = 5020

fórmula usada
R= Dirección = Base + i * tam_tipo
*//
